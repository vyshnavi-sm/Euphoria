<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed - Your Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #5D4037;
            --primary-light: #8D6E63;
            --primary-dark: #321911;
            --text-dark: #2C1810;
            --text-light: #8D6E63;
            --background-light: #F5F5F5;
            --gold: #D4AF37;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: var(--background-light);
            color: var(--text-dark);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .failure-container {
            max-width: 800px;
            margin: 60px auto;
            padding: 40px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .failure-header {
            margin-bottom: 30px;
        }

        .section-subtitle {
            color: var(--text-light);
            font-size: 18px;
            margin-bottom: 10px;
        }

        .section-title {
            color: var(--primary-color);
            font-family: 'Playfair Display', serif;
            font-size: 36px;
            margin-bottom: 20px;
        }

        .failure-content {
            margin-bottom: 40px;
        }

        .failure-icon {
            font-size: 80px;
            color: #dc3545;
            margin-bottom: 20px;
        }

        .failure-message {
            font-size: 24px;
            color: var(--text-dark);
            margin-bottom: 20px;
        }

        .order-details {
            background-color: var(--background-light);
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 30px;
        }

        .order-number {
            font-size: 18px;
            color: var(--primary-color);
            margin-bottom: 10px;
        }

        .button-group {
            display: flex;
            gap: 20px;
            justify-content: center;
        }

        .btn-brown {
            background-color: var(--primary-color);
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 5px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .btn-brown:hover {
            background-color: var(--primary-dark);
            color: white;
        }

        .btn-outline-brown {
            background-color: transparent;
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
            padding: 12px 30px;
            border-radius: 5px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .btn-outline-brown:hover {
            background-color: var(--primary-color);
            color: white;
        }

        @media (max-width: 768px) {
            .failure-container {
                margin: 40px 20px;
            }
            
            .button-group {
                flex-direction: column;
                align-items: center;
            }
            
            .btn-brown, .btn-outline-brown {
                width: 100%;
                max-width: 300px;
            }
        }
    </style>
</head>
<body>
    <%- include("../../views/partials/user/header") %>

    <div class="failure-container">
        <div class="failure-header">
            <p class="section-subtitle">Payment Failed</p>
            <h2 class="section-title">Oops! Something went wrong</h2>
        </div>
        
        <div class="failure-content">
            <div class="failure-icon">
                <i class="fas fa-times-circle"></i>
            </div>
            
            <h3 class="failure-message">Your payment could not be processed</h3>
            
            <div class="order-details">
                <p class="order-number">Order Number: #<%= orderId %></p>
                <p>Please try again or choose a different payment method.</p>
            </div>

            <div class="button-group">
                <button onclick="retryPayment()" class="btn btn-brown">
                    <i class="fas fa-redo"></i> Retry Payment
                </button>
                <a href="/user/orders/<%= orderId %>" class="btn btn-outline-brown">
                    <i class="fas fa-file-alt"></i> View Order
                </a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <script>
        function retryPayment() {
            window.location.href = '/user/checkout?orderId=<%= orderId %>';
        }
    </script>
</body>
</html> 