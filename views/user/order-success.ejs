<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Success - Euphoria</title>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&family=Montserrat:wght@300;400;500&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    :root {
      --primary-color: #5D4037;
      --primary-light: #8B6B61;
      --primary-dark: #321911;
      --accent-color: #D7CCC8;
      --accent-dark: #A1887F;
      --text-light: #F5F5F5;
      --text-dark: #3E2723;
      --background-light: #EFEBE9;
      --gold: #D4AF37;
    }
    
    body {
      background-color: var(--background-light);
      font-family: 'Cormorant Garamond', serif;
      color: var(--text-dark);
      line-height: 1.6;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }
    
    .success-container {
      max-width: 800px;
      margin: 80px auto 50px;
      background: #fff;
      border-radius: 15px;
      box-shadow: 0 15px 35px rgba(50, 25, 17, 0.1);
      padding: 0;
      overflow: hidden;
      text-align: center;
    }
    
    .success-header {
      background-color: var(--primary-color);
      color: var(--text-light);
      padding: 30px;
      text-align: center;
      border-bottom: 3px solid var(--gold);
    }
    
    .success-content {
      padding: 40px;
    }

    .section-title {
      font-size: 32px;
      color: var(--text-light);
      margin-bottom: 0;
      font-family: 'Playfair Display', serif;
      letter-spacing: 1px;
    }
    
    .section-subtitle {
      font-size: 14px;
      color: var(--accent-color);
      font-family: 'Montserrat', sans-serif;
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    .success-icon {
      font-size: 80px;
      color: var(--gold);
      margin-bottom: 20px;
    }

    .success-message {
      font-family: 'Playfair Display', serif;
      font-size: 28px;
      color: var(--primary-color);
      margin-bottom: 20px;
    }

    .order-details {
      background-color: var(--background-light);
      padding: 20px;
      border-radius: 10px;
      margin: 30px 0;
    }

    .order-number {
      font-family: 'Montserrat', sans-serif;
      font-size: 16px;
      color: var(--primary-dark);
    }

    .btn-brown {
      background-color: var(--primary-color);
      color: var(--text-light);
      border: none;
      padding: 12px 25px;
      border-radius: 8px;
      font-weight: 500;
      font-family: 'Montserrat', sans-serif;
      transition: 0.3s;
      letter-spacing: 1px;
      text-transform: uppercase;
      font-size: 14px;
      margin: 10px;
    }

    .btn-brown:hover {
      background-color: var(--primary-dark);
      color: var(--text-light);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(62, 39, 35, 0.2);
    }

    .btn-outline-brown {
      background-color: transparent;
      color: var(--primary-color);
      border: 2px solid var(--primary-color);
      padding: 12px 25px;
      border-radius: 8px;
      font-weight: 500;
      font-family: 'Montserrat', sans-serif;
      transition: 0.3s;
      letter-spacing: 1px;
      text-transform: uppercase;
      font-size: 14px;
      margin: 10px;
    }

    .btn-outline-brown:hover {
      background-color: var(--primary-color);
      color: var(--text-light);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(62, 39, 35, 0.2);
    }

    .illustration {
      max-width: 300px;
      margin: 0 auto 30px;
    }

    .button-group {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 15px;
      margin-top: 30px;
    }

    @media (max-width: 768px) {
      .success-container {
        margin: 40px 20px;
      }
      
      .button-group {
        flex-direction: column;
        align-items: center;
      }
      
      .btn-brown, .btn-outline-brown {
        width: 100%;
        max-width: 300px;
      }
    }
  </style>
</head>
<body>
  <%- include("../../views/partials/user/header") %>

  <div class="success-container">
    <div class="success-header">
      <p class="section-subtitle">Your Order is Complete</p>
      <h2 class="section-title">Thank You!</h2>
    </div>
    
    <div class="success-content">
      <div class="success-icon">
        <i class="fas fa-check-circle"></i>
      </div>
      
      <h3 class="success-message">Your order has been placed successfully!</h3>
      
      <div class="order-details">
        <p class="order-number">Order Number: #<%= orderId %></p>
        <p>A confirmation email has been sent to your registered email address.</p>
      </div>

      <div class="button-group">
        <a href="/user/orders" class="btn btn-brown">
          <i class="fas fa-file-alt"></i> View Orders
        </a>
        <a href="/shop" class="btn btn-outline-brown">
          <i class="fas fa-shopping-bag"></i> Continue Shopping
        </a>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>
</html> 